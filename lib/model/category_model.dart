import 'package:flutter/material.dart' ;
class CategoryModel extends StatelessWidget {
  const CategoryModel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
